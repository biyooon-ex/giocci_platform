services:
  giocci_relay:
    image: ghcr.io/biyooon-ex/giocci_platform/giocci_relay:0.4.1
    build:
      context: ../../
      dockerfile: ./apps/giocci_relay/Dockerfile
      platforms:
        - "linux/amd64"
        - "linux/arm64"
      args:
        ELIXIR_VERSION: ${ELIXIR_VERSION}
        ERLANG_VERSION: ${ERLANG_VERSION}
        UBUNTU_VERSION: ${UBUNTU_VERSION}
    volumes:
      - ./config/giocci_relay.exs:/app/giocci_relay.exs:ro
      - ./config/zenoh.json5:/app/zenoh.json5:ro
    environment:
      - ZENOHD_CONNECT_ENDPOINTS
    restart: "always"
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "5"
